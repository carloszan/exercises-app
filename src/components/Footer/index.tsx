import React from "react";
import { Container, Card } from "react-bootstrap";

const Footer = () => {
  return (
    <footer>
      <Container>
        <Card.Title className="mb-2 text-muted">Czar Company</Card.Title>
      </Container>
    </footer>
  );
};

export default Footer;
